<nav class="navbar border-bottom">
  <div class="container">
    <%= link_to "Brand Image", client_root_path, class:"navbar-brand" %>

    <div class="d-flex flex-row align-items-center justify-content-center">
      <%= link_to "Coins", "", class: "nav-link me-3" %>
      <%= link_to "Claim", "", class: "nav-link me-3" %>
      <%= link_to "Language", "", class: "nav-link me-3" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasDarkNavbar" aria-controls="offcanvasDarkNavbar" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>


    <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasDarkNavbar" aria-labelledby="offcanvasDarkNavbarLabel">
      <% if client_user_signed_in? %>
        <div class="offcanvas-header mt-2 border-bottom">

          <div class="d-flex flex-column w-100">
            <div class="d-flex flex-row align-items-center mb-2">
              <div class="border-end p-3">
                <% if current_client_user.image.present? %>
                  <%= image_tag current_client_user.image.url, class: "border rounded-circle" , style: "width: 65px; height: 65px; object-fit: cover;" %>
                <% else %>
                  <i class="fa-solid fa-user fa-2xl"></i>
                <% end %>
              </div>
              <div class="d-flex flex-column ms-2">
                <span>Username: <%= current_client_user.username %></span>
                <span>Email: <%= current_client_user.email %></span>
                <span>Coins: <%= current_client_user.coins %></span>
              </div>
            </div>

            <%= link_to "Cash In Now", "", class: "btn btn-primary w-100" %>
          </div>
        </div>

        <div class="offcanvas-body">
          <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="#">Contact Us</a>
            </li>
            <li class="nav-item">
              <%= link_to 'My Profile', me_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign out', destroy_client_user_session_path, data: { 'turbo-method': :delete }, class: "nav-link" %>
            </li>
          </ul>

        </div>
      <% end %>

    </div>
  </div>
</nav>