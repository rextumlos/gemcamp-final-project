<div class="vh-100 vw-100">
  <%= render partial: 'admin/shared/top_nav' %>

  <div class="d-flex flex-row h-100 w-100">
    <%= render partial: 'admin/shared/side_nav' %>

    <div class="p-4 w-100 mb-4">
      <div class="d-flex flex-row align-items-center">
        <%= link_to 'Back', news_tickers_path, class: "btn btn-primary me-2"%>
        <span class="fs-3 fw-semibold">Edit NewsTicker</span>
      </div>
      <div class="w-100 border-bottom mx-2"></div>
      <%= render partial: 'form', locals: { news_ticker: @news_ticker, url: news_ticker_path(@news_ticker), method: :patch } %>
    </div>
  </div>
</div>

